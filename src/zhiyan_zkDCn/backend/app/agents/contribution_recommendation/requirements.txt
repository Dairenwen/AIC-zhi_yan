# 智研 · 投稿推荐 Agent — 依赖

# ── 核心 ──
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0

# ── 模型服务 ──
requests>=2.31.0
# sentence-transformers>=2.7.0  # 可选（本地嵌入）
# FlagEmbedding>=1.2.0          # 可选（本地重排）

# ── 数据处理 ──
numpy>=1.24.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── 检索（可选） ──
# pymilvus>=2.4.0
# elasticsearch>=8.12.0

# ── API Server ──
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.0.0

# ── 数据库 ──
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL 驱动
# 无 PG 时自动降级使用 SQLite（Python 内置）

# ── 开发 ──
pytest>=8.0.0
pytest-asyncio>=0.24.0
